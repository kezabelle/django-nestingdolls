{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test tests/test_sequence.mjs"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2"
  }
}
